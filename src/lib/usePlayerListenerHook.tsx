import Player from "shaka-player";
import {ShakaReactWrapperProps} from "../types";

const usePlayerListenerHook = (player: Player, props?: ShakaReactWrapperProps) => {
}
export default usePlayerListenerHook;